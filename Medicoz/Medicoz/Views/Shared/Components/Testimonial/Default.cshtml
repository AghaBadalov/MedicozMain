@model List<Testimonial>

<div class="testimonial-outer">
    <!-- Client Testimonial Carousel -->
    <div class="client-testimonial-carousel owl-carousel owl-theme">

        @foreach (var item in Model)
        {
            <!--Testimonial Block -->
            <!--Testimonial Block -->
            <div class="testimonial-block">
                <div class="inner-box">
                    <div class="text" style="margin-left:180px; margin-right:180px">@item.Desc</div>
                </div>
                <div class="thumb-item" style="display:flex;align-items:center;justify-content:center;flex-direction:column">
                    <figure class="thumb-box"><img style="width:100px;height:100px;border-radius:100%" src="~/uploads/testimonials/@item.ImageUrl" alt=""></figure>
                    <div class="author-info" style="text-align">
                        <div class="author-name">@item.Name</div>
                        <div class="designation">@item.Position</div>
                    </div>
                </div>
            </div>
        }
    </div>

    <div class="client-thumb-outer">
        <div class="client-thumbs-carousel owl-carousel owl-theme">
        </div>
    </div>
</div>
